include ../DoolpConfig
MOD=tests
MODULENEEDS = # ../libDoolp
MODULESUBS =
MODULECOMPILEFLAGS = -I../include/ -I./ -D__GLOG_SHOW_LOG
# MODULELIBNAME=lib$(MOD).so
MODULELINKFLAGS = -L../libDoolp -lDoolp -lDML # -shared
# MODULERUNEXTERN = doolpAgent sampleA.conf.dml
# UseDoolpPreproc = yes
DEFAULTACTION = run# lib runExtern
